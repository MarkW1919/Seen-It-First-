# Seen-It-First-Edge Dependencies
# Target: NVIDIA Jetson Orin Nano Super, Python 3.10+

# Core
numpy>=1.24,<2.0
opencv-python-headless>=4.8
PyYAML>=6.0

# Inference (Jetson JetPack provides TensorRT + PyCUDA system-wide)
# tensorrt          # provided by JetPack
# pycuda            # provided by JetPack

# ONNX Runtime — vehicle classifier, color detector, ReID embedding models
# Use onnxruntime-gpu on Jetson (ships with JetPack 6+); on x86 use onnxruntime
onnxruntime>=1.16
onnx>=1.14

# Tracking
scipy>=1.10

# HTTP requests (geocoder, router, model download fallback)
requests>=2.31

# Navigation API server
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0
httpx>=0.27          # async HTTP for FastAPI test client + geocoder async option

# Audio alerts (optional, system packages preferred)
# beep / sox / aplay available via apt on Jetson
