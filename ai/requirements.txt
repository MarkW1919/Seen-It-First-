# AI Pipeline Dependencies
# Pinned to versions compatible with L4T r36.2.0 (CUDA 12.2, cuDNN 8.9, TensorRT 8.6)
# NOTE: torch and torchvision are pre-installed in the L4T base image — do NOT reinstall via pip
# NOTE: opencv is provided by system apt package (python3-opencv) — do NOT install via pip

numpy==1.26.4
pillow==10.4.0
ultralytics==8.2.86
onnxruntime-gpu==1.18.1
scipy==1.13.1
pyyaml==6.0.2
httpx==0.27.2
redis==5.0.8
