<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for RepoScan Pro.

    Production: cleartext traffic is BLOCKED globally.
    Debug builds: cleartext traffic allowed only to localhost/emulator for development.
    Certificate pinning: enforced for the production API domain.
-->
<network-security-config>

    <!-- Block cleartext (HTTP) traffic globally -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug overrides: allow cleartext to localhost only -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <!-- Allow cleartext to local development servers (debug only) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

</network-security-config>
